import { View, Text } from "react-native";
import React from "react";

const ServeScreen = ({ navigation }) => {
  return (
    <View>
      <Text>ServeScreen</Text>
    </View>
  );
};

export default ServeScreen;
